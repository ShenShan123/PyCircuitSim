# PyCMG Conda Environment
# Create with: conda env create -f environment.yml
# Activate with: conda activate pycmg

name: pycmg
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - numpy>=1.24.0
    - pytest>=8.0.0
