# NOTE: This file is kept for historical reference only.
#
# The `neural_network` package lives inside PyCircuitSim and uses the top-level
# requirements.txt. Install dependencies from the project root:
#
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#   pip install torch -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# The original frozen environment from the BSIM-AR paper (torch 2.5 +
# cu121, pandas, scikit-learn 1.7, scipy 1.15, etc.) is archived in git
# history if you need to reproduce the paper's original experiments
# byte-for-byte.
#
# Runtime dependencies actually imported by neural_network/:
#   numpy
#   scipy.ndimage, scipy.signal.windows, scipy.sparse
#   matplotlib
#   scikit-learn
#   torch (+ torch.utils.data, torch.optim, torch.optim.lr_scheduler)
#   pycmg  (sibling package under external_compact_models/)
