# PyCircuitSim full-terminal NN runtime and training dependencies.
#
# Install with:
#   http_proxy=http://127.0.0.1:2080 https_proxy=https://127.0.0.1:2080 \
#     pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# PyTorch is intentionally NOT pinned here — install the CPU or CUDA build
# that matches your hardware. For the pycircuitsim conda env we currently
# use torch 2.10.0. Install through the configured proxy and Tsinghua mirror:
#   http_proxy=http://127.0.0.1:2080 https_proxy=https://127.0.0.1:2080 \
#     pip install torch -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# Tsinghua mirror (recommended for users in China — see AGENTS.md):
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# ── Numerical / plotting core ───────────────────────────────────────────────
# numpy 2.x is required (installed env uses 2.4.2); 1.26 is the last 1.x line
# that is API-compatible with our code and is a safe lower bound.
numpy>=1.26
# scipy.sparse (lil_matrix, csr, spsolve) — the sparse MNA solver.
# scipy.ndimage / scipy.signal.windows — LDS smoothing in neural_network.losses.
scipy>=1.11
matplotlib>=3.7

# ── scikit-learn ────────────────────────────────────────────────────────────
# sklearn.preprocessing.KBinsDiscretizer — LDS weight binning in
#   neural_network.losses.bni_mae.compute_lds_weights_per_target
# sklearn.metrics.r2_score — scatter plots in neural_network.eval.visualization
scikit-learn>=1.3

# ── pybind11 ────────────────────────────────────────────────────────────────
# Required by the PyCMG submodule (used by pycircuitsim.models.mosfet_cmg
# and the NN data generator at external_compact_models/bsim_cmg/scripts/).
pybind11>=2.11

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=7.0

# ── PyTorch (install separately) ─────────────────────────────────────────────
# The LEVEL=75 (DirectNet-Full) and LEVEL=76 (BSIM-AR-Full) models are
# PyTorch-backed. Installing torch is required to run NN inference or the
# bsimar training CLI, but the solver itself (LEVEL=72 + passives) works
# without it.
#
#   http_proxy=http://127.0.0.1:2080 https_proxy=https://127.0.0.1:2080 \
#     pip install 'torch>=2.2' -i https://pypi.tuna.tsinghua.edu.cn/simple
