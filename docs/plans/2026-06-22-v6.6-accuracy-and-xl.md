# V6.6 — NN accuracy improvement + XL capacity tier

Date: 2026-06-22 · Branch: `feat/ac-analysis` · Author: campaign notes

> **STATUS: V6.6 CLOSED (committed `233238d`).** Both simple-first levers are
> decisive NEGATIVES: XL capacity over-fits (curve peaks at `large` 12/16,
> regresses to 9/16 at XL), and the µA-band loss lever is inert (switchcap is
> charge/transient-owned, not DC-loss-owned). The **entire simple-lever space is
> now exhausted.** §1–4 are the campaign record; **§5 is the handoff/roadmap for
> the next session** — read that first.

## 1. What the V6.4.9 / V6.5 benchmark established (the starting point)

`results/benchmark_sml/REPORT.md` (S/M/L capacity study + V6.5 AC) is unusually
decisive about *where the accuracy actually lives*:

- **Device-level fidelity is already excellent at every capacity.** DC Id-Vgs +
  inverter VTC/transient mean NRMSE < 4 % (most < 2 %); inverter 16/16 PASS at
  all sizes/techs. AC **gain** (autograd gm/gds) err < 1.5 dB on **24/24** cells.
  Capacity barely moves device curves and at the finer nodes `large` slightly
  *overfits* (tsmc12 DC 0.36 % → 1.25 % medium→large). **Device fidelity is NOT
  the bind.**
- **Circuit pass-rate rises monotonically with capacity: 6 → 9 → 12 / 16**
  (S → M → L). The gains come from charge-transfer / timing circuits.
- **Two gaps survive capacity** and were already root-caused by the V6.4.x
  campaigns:
  1. **opamp open-loop gain collapse** (S+M all techs; recovers at L only for
     tsmc5/tsmc12). Owned by the **DC value-surface** (NR lands a low-gain
     basin). V6.4.7/8 exhausted cheap-lever, capacity (S1), derivative-via-loss
     (S10), solver-continuation (S2, the one win) and structural-EKV (S3).
  2. **tsmc5 switchcap ~11.7–12.1 % over-conduction.** Owned by **µA-band loss
     compression**: the asinh output norm uses a per-target geometric-mean scale
     `s_id`, and we measured `s_id ≈ 1.0–2.6e-5 A` for *every* tech. Currents
     below that knee sit in the asinh-**linear** band where `asinh(id/s_id) ≈
     id/s_id`, so a *relative* error at, say, 2 µA contributes almost no loss
     mass. The switchcap charges a 100 fF cap through a transmission gate over a
     1.9 ns window — the pass devices conduct squarely in that compressed µA
     band, so the net over-conducts.
- **AC RHP-zero phase** (Cgd transcapacitance feed-forward) is not reproduced —
  structural, diagnostic-only, not gated.

The report's bottom line, and the V6.4.8 close, name the next campaign
explicitly: **"µA-band loss/normalization de-compression"** for the switchcap;
the opamp/value-surface and AC-phase gaps are *not* reachable by simple levers
("no retraining warranted").

## 2. Ranked improvement suggestions (simple → medium; no complex methods)

| # | Lever | Class | Targets | Risk |
|---|---|---|---|---|
| 1 | **XL capacity tier (512×8, 2.13 M p)** | simple / mechanical | extend the 6→9→12 capacity curve | low (slight device overfit; bounded by EMA + early-stop) |
| 2 | **µA-band loss de-compression** — reuse the existing default-off `SubthresholdIdLoss` retuned from the sub-nA band to the **µA band** (`s2=1e-7, upper=3e-5`, λ screened 0.02/0.05) | simple→medium / config-only | tsmc5 switchcap over-conduction (the one *loss-owned* device gap) | low — additive term, default-off → stock checkpoints byte-identical; A/B-gated before rollout |
| 3 | EMA + early-stop already in recipe; rely on them (not a new weight-decay schedule) to bound XL device overfit | simple | XL overfit | low |
| — | **opamp gain collapse / AC RHP-zero phase** | NOT pursued | value-surface / transcapacitance-feedforward | prior campaigns proved these are not simple-lever-reachable; report says no retrain warranted |

Why #2 is the right "simple" lever and not a normalization-knee hack: capping
`s_id` globally would also re-weight the near-zero / sign-flipping rows and can
disturb the strong-inversion fit, which is exactly why the project built
`SubthresholdIdLoss` as an *additive* term. Retuning its band from
`s2=1e-9, upper=1e-6` (sub-nA, the SRAM target) to `s2=1e-7, upper=3e-5` (µA)
makes the loss decade-balanced (≈ log) from 0.1–30 µA, so the Huber term
penalizes the over-conduction **ratio** directly while leaving > 30 µA strong
inversion to the base MAE. It is a two-flag config change, no model code, and
cannot regress the default-off shipping checkpoints.

**Datasets:** the clean V6.4.9 datasets (full Vth + geometry, on disk) are
current. Both levers are loss/architecture changes, not data changes → **no
regeneration needed.**

## 3. Execution  — ALL STEPS DONE (see §4 for outcomes)

1. **XL infra** (done): `("direct","xl")` preset, `--size xl`, parser cascade
   slot, benchmark scripts (`TECHS/SIZES/DEVS` + `EXTRA_TRAIN_ARGS` env;
   collector `SIZES` data-driven).
2. **A/B-validate lever** (medium, tsmc5 = target, tsmc12 = no-regress control,
   λ ∈ {0.02, 0.05}): gate on switchcap `charge_err` ↓ AND DC NRMSE not
   regressed. Decide adopt/revert (commit-first, keep-if-helps).
3. **Full campaign** on 3× RTX 4090: XL for all techs; S/M/L retrained with the
   lever **iff** step 2 adopts it (else add XL only, keep stock S/M/L).
4. **Re-evaluate all** → regenerate `REPORT.md` with XL column + detailed
   metrics; compare to the git-committed V6.4.9/V6.5 baseline.
5. CHANGELOG V6.6 + CLAUDE.md + commit. Record the lever outcome (keep or
   reverted dead-end) per project discipline.

Baseline insurance: the 24 V6.4.9 checkpoints are tarred to
`results/v6_6_uA_ab/baseline_ckpt_backup/` (checkpoints are gitignored) and the
current `REPORT.md` is committed before any retrain.

## 4. Results

### Lever #2 (µA-band loss de-compression) — KILL

A/B on medium (tsmc5 = target, tsmc12 = no-regress control), λ ∈ {0.02, 0.05},
`SubthresholdIdLoss` retuned to `s2=1e-7, upper=3e-5`:

| arm | tsmc5 switchcap charge_err | tsmc5 DC no-regress |
|---|---|---|
| stock medium | **11.84 %** (lvl 0.3717 V, NG 0.2948 V) FAIL | clean |
| µA λ=0.05 | **12.05 %** (0.3731 V) FAIL — worse | slight regress |
| µA λ=0.02 | **11.69 %** (0.3707 V) FAIL — flat | slight regress (l_16nm base NRMSE 0.84→2.41 %) |
| tsmc12 λ=0.05 | 4.19 → 4.24 % PASS (no regress) | clean |

The charge level moved < 0.2 % of VDD despite the aux term running ≈ ½ the base
MAE and visibly reshaping the DC fit. **The over-charge survives direct µA-band
loss de-compression** — exactly as it survived the V6.4.8-S3 EKV structural
prior. This *refutes* the V6.4.8 roadmap's "loss-compression-owned" hypothesis:
the tsmc5 switchcap over-charge is **not** µA-band-DC-current-owned. It is a
sample-and-hold **charge/transient** behaviour (the cap charges through the
transmission gate; the DC Id surface is already < 1 % accurate in that band).
Fixing it needs a transient/charge-model investigation — a *complex* lever the
user de-scoped. **Lever reverted** (temp `_uA*` checkpoints removed from the
resolver dir; default-off `SubthresholdIdLoss` infra unchanged and recoverable).

### Lever #1 (XL capacity tier) — OVER-FIT (kept as boundary, not production)

All 8 XL checkpoints (512×8, 2.13 M p) trained on the clean recipe (3× RTX 4090,
~3 h wall, best val ≈ 2e-4 — ~10× lower than medium's ~2e-3). Re-evaluated all
four tiers together (S/M/L eval logs reused — same checkpoints + test code).

| metric | small | medium | large | **xl** |
|---|---|---|---|---|
| complex gates PASS | 6/16 | 9/16 | **12/16** | **9/16** ↓ |
| device mean parametric NRMSE % | 1.63 | **1.29** | 1.7 | **2.17** ↑ (worst) |
| AC device CS-amp PASS | 5/12 | 4/12 | 4/12 | 4/12 |
| AC opamp PASS | 0/4 | 0/4 | 0/4 | 0/4 |

**The capacity curve is NON-monotonic — it peaks at `large` and declines at XL.**
XL fits the training distribution tightest (lowest val loss) but generalizes
*worst* to off-nominal geometry/VT sweeps (e.g. tsmc7 DC/pmos 0.51 %→2.65 %,
tsmc12 DC/pmos 0.76 %→3.57 %) and **loses every value-surface-fragile gate
`large` won**: tsmc5 opamp, tsmc12 opamp, tsmc7 ring-osc all flip PASS→FAIL.
Lower val loss ≠ better circuits — the opamp/RO gates are NR-fixed-point
properties of the value-surface (cf. [[v647-s10-deriv-fidelity-vs-opamp]]).
**Cleanest confirmation yet of V6.4.8-S1 "capacity is not the bind."**

**Decision:** `large` is the capacity sweet spot. **`medium` stays the shipped
production size** (V6.4.7 mix untouched); XL is retained as the documented
over-fit boundary (its 8 checkpoints sit in the `tsmc{X}_dn_xl_*` slots, not in
any production resolver path). Net pass-rate change vs V6.4.9: **none** — both
V6.6 levers are negatives.

## 5. Next session — handoff & roadmap

**Read this first.** The simple-lever space is exhausted. The negative-lever
ledger (do NOT re-try these — each is an empirically-closed dead end):

| lever | verdict | why it can't work |
|---|---|---|
| capacity ↑ (S1 `large`, V6.6 `xl`) | KILL | over-fits value-surface past `large`; bigger collapses opamp/RO |
| µA-band loss de-compression (V6.6) | KILL | switchcap is charge/transient-owned, not DC-current-band-owned |
| deriv-via-loss / Sobolev ∂id/∂V (S10) | KILL | improving the id-slope collapses the value-owned opamp |
| structural EKV id-core (S3) | KILL | value-surface-neutral on SC; regresses opamp locus |
| cheap data/loss reweighting, floor-k (S0) | KILL | gds cancels at the NR fixed point |
| solver continuation (S2) | the ONE win | path-preservation, already shipped |

### Remaining gaps, with ownership now firmly established

1. **tsmc5 switchcap ~11.2 % over-charge — CHARGE / TRANSIENT-owned.** This is
   the most tractable remaining gap and the best-localized: V6.6 proved it is
   NOT DC-Id (the surface is <1 % accurate there and the loss lever was inert).
   **Leading hypothesis for next session:** the over-charge is the *transient
   companion* of the AC cap under-prediction. The V6.5 AC study found tsmc5 NMOS
   under-predicts its output cap (f3db ratio **1.58**); a device that presents
   too *little* capacitance charges the 100 fF hold cap too *fast* → over-charge.
   The NN caps are autograd `dQ/dV` of the predicted terminal charges, so the
   root is the **charge (Q) head fidelity**, not `id`.
   - **Test:** at the switchcap sample-window operating points, compare the NN's
     `qg/qd/qs/qb` and the resulting `Cgg/Cgd/Cgs/Cdg/Cdd` (autograd) against the
     OSDI op-vars. Expect a systematic cap under-prediction correlating with the
     over-charge across techs (tsmc5 worst, tsmc12/16 PMOS also under-predict).
   - **Candidate lever (medium):** a **charge / cap-fidelity loss** — Sobolev on
     the *charge* columns (the V5 Phase-C 8-channel `dQ/dV` form, recoverable from
     `git show 930c274`; it was net-detrimental at S-scale for `id`, but the
     charge channels were never isolated) OR up-weight the `q*`/`c*` columns
     (loss-preset `e3` down-weights them today — invert that). Gate on switchcap
     `charge_err` + the AC f3db ratio, no DC regress.
   - **Cheaper first probe:** confirm the hypothesis with a *charge diagnostic*
     before any retrain (reuse `tests/common/complex_ac.py` cap extraction at the
     SC bias). If caps are accurate there, the hypothesis is wrong → look at the
     transient integrator / `_apply_vds_correction` on the fast sampling edge.

2. **tsmc7 / tsmc16 opamp (+ the L→XL opamp collapse) — DC value-surface basin.**
   Not capacity, not deriv-loss. The *shipping* tsmc7/tsmc16 already use the
   special **pivcor / s12cor** recipes (V6.4.7) to hold the high-gain basin; the
   clean single recipe doesn't. Next-session options are all *complex* (the brief
   de-scoped them): per-tech recipe specialization, value-surface-targeted data
   harvesting around the opamp OP, or monotonicity/spectral-norm network
   constraints. Lowest-risk: extend the pivcor/s12cor recipe family. See
   [[v648-s2-continuation-first-opamp]], [[v648-gds-floor-inert-on-opamp-gain]].

3. **AC RHP-zero phase (Cgd feed-forward)** — structural transcapacitance limit,
   diagnostic-only, not gated. Out of scope unless a Miller-zero-aware cap model
   is built. See [[v65-nn-ac-accuracy]].

4. **tsmc5 ring-osc (period err 6–14 %)** and **inverter VTC MaxErr ≤25 mV** —
   low-VDD timing / steep-trip metrics; never met; likely entangled with #1/#2.

### Recommended next campaign (single focus)

**Attack gap #1 (switchcap charge model) — it is the only remaining gap with a
concrete, falsifiable hypothesis and a clear gate.** Start with the cheap charge
diagnostic (no training); if it confirms cap under-prediction, run the
charge-fidelity loss as an A/B on tsmc5 medium (target) + tsmc12 (no-regress),
exactly like the V6.6 µA A/B harness (`scripts/v6_6_uA_ab_eval.sh`). If the
diagnostic refutes it, pivot to the transient integrator. Do **not** reopen the
opamp/value-surface track with simple levers.

### Pointers / provenance

- Production size = `medium`; V6.4.7 shipping mix (pivcor/s12cor) untouched.
- XL checkpoints: `external_compact_models/bsimar/checkpoints/tsmc{X}_dn_xl_{dev}_best.pt`.
- Killed µA-lever checkpoints parked: `results/v6_6_uA_ab/killed_lever_ckpt/`.
- V6.4.9 baseline ckpt backup (rollback): `results/v6_6_uA_ab/baseline_ckpt_backup/`.
- 4-tier metrics: `results/benchmark_sml/REPORT.md` (+ `benchmark_data.json`).
- Train harness: `scripts/benchmark_train_sml.sh` (`SIZES/TECHS/DEVS` +
  `EXTRA_TRAIN_ARGS` env; always pass `--force` or rely on the `noforce`
  placeholder — the `xargs -L1` trailing-blank trap is fixed but be aware).
  Eval: `scripts/benchmark_run_tests.sh`; collect: `scripts/benchmark_collect.py`.
- Default-off, recoverable infra to reuse: `SubthresholdIdLoss` (µA-band retune),
  `SobolevIdLoss` (extend to charge channels per #1), `_EKVCore`, EMA/SWA.
