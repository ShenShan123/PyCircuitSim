"""Gates for one device at one bias — the compact-model surface itself.

Mirrors ``circuit_templates/L0_devices/``. A failure here is a model
failure: there is no circuit around the device to blame.
"""
